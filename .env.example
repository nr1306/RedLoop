# Copy to .env and fill in. .env is gitignored.
OPENAI_API_KEY=

# Model the target agent runs on
TARGET_MODEL=gpt-5.4

# Hard cap on agent-loop turns per conversation (one turn = one model call)
TARGET_MAX_TURNS=8
