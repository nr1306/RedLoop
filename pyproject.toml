[project]
name = "redloop"
version = "0.0.1"
requires-python = ">=3.11"

[tool.pytest.ini_options]
# Lets tests do `from target import ...` without installing the package
pythonpath = ["."]
testpaths = ["tests"]
